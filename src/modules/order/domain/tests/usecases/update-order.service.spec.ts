describe('Domain_Usecase_UpdateOrder', () => {
  it('should update the order status', () => {});
  it('should throw if trying to update to an invalid status', () => {});
  it('should emit event on status update', () => {});
});
