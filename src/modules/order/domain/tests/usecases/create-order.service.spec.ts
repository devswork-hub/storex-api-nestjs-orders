describe('Domain_Usecase_CreateOrder', () => {
  it('should create an order with valid data', () => {});
  it('should not create an order with invalid items', () => {});
  it('should apply discount if conditions are met', () => {});
  it('should emit event after order is created', () => {});
});
