describe('Domain_Usecase_DeleteOrder', () => {
  it('should delete an existing order', () => {});
  it('should throw if order does not exist', () => {});
  it('should not allow deletion of already paid order', () => {});
});
